# Paxos
Paxos-protocol

# Setup

1) Go to {path}/Paxos/
2) mkdir build
3) cd build
4) cmake ..

# Build and Execute

5) make
6) ./src/Paxos

# Tests

Uses Check. 
Execute with make test in build folder. 